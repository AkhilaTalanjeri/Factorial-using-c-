#include<stdio.h>
void main
 {
  int num,fact=1,i;
  printf("enter the number");
  scanf("%d",&num);
  while(num!=0)
    {
    fact=fact*num;
    num--;
    }
  printf("factorial of %d is %d",num,fact);
}
